export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // eslint-disable-next-line because no-constant-condition
  if (true) {
    resolve();
  } else {
    reject();
  }
  });
}
